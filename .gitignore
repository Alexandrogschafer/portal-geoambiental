# Dados brutos (shapefiles) — não versionar se forem grandes/sensíveis
data/raw/*
!data/raw/.gitkeep

# Sistema
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Cache local do osmnx (respostas da Overpass API) — scripts/extract_osm_viario.py
cache/
